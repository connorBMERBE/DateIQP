import pandas as pd
import sql.connector as sql 

def dbViewFiltered(): 

    cursor.execute(f'')    


cursor.execite("""SELECT 
    *
FROM
    dates
WHERE
    class = value_of_interest
""")


{'imageAddress':-1, 
            'harvestDay':harvestDay.strftime('%Y-%m-%d'), 
            'measureDay':measureDay.strftime('%Y-%m-%d'), 
            'barCode':barCode, 
            'weight':weight }


